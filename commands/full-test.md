---
description: Run comprehensive web testing across all roles, modules, features, platforms, and modes. Usage: /full-test
---

Invoke the `web-test-orchestrator` skill to run a full web testing session.

Pass any arguments the user provided after `/full-test` as context to the skill (e.g. target URL, specific modules to focus on).
